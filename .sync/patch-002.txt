auge,
   Cpu,
   LoaderCircle,
+  Download,
   PlayCircle,
+  RefreshCw,
+  Square,
   ShieldCheck,
   Wifi
 } from "lucide-react";
@@ -30,10 +33,16 @@ export interface InspectorProps {
   frame: SimulationSnapshot | null;
   config: ScenarioConfig;
   busy: boolean;
+  dirty: boolean;
   onParameterChange: (path: DashboardParameterKey, value: number) => void;
   onAlgorithmChange: (algorithm: AlgorithmMode) => void;
   onPresetChange: (preset: DashboardPreset) => void;
+  onSeedChange: (seed: number) => void;
+  onRerun: () => void;
+  onExportScenario: () => void;
+  onExportRun: () => void;
   onRunMonteCarlo: ((count: number) => void) | undefined;
+  onCancelMonteCarlo: (() => void) | undefined;
   monteCarloSummary: MonteCarloSummary | null;
   busyMonteCarlo: boolean;
 }
@@ -556,23 +565,68 @@ function OutcomeBadge({ run }: { run: SimulationRun }) {
 
 function ExperimentInspector({
   run,
+  config,
   busy,
+  dirty,
+  onSeedChange,
+  onRerun,
+  onExportScenario,
+  onExportRun,
   onRunMonteCarlo,
+  onCancelMonteCarlo,
   monteCarloSummary,
   busyMonteCarlo
-}: Pick<InspectorProps, "run" | "busy" | "onRunMonteCarlo" | "monteCarloSummary" | "busyMonteCarlo">) {
-  const [sampleCount, setSampleCount] = useState(50);
+}: Pick<
+  InspectorProps,
+  "run" | "config" | "busy" | "dirty" | "onSeedChange" | "onRerun" |
+  "onExportScenario" | "onExportRun" | "onRunMonteCarlo" |
+  "onCancelMonteCarlo" | "monteCarloSummary" | "busyMonteCarlo"
+>) {
+  const [sampleCount, setSampleCount] = useState(5);
   const maxLoad = useMemo(() => {
     if (monteCarloSummary === null || monteCarloSummary.variants.length === 0) return 1;
-    return Math.max(1, ...monteCarloSummary.variants.map((variant) => variant.p95PeakLoadG));
+    return Math.max(1, ...monteCarloSummary.variants.map((variant) => variant.p95PeakLoadKn));
   }, [monteCarloSummary]);
 
   return (
     <div className="inspector-scroll">
+      <section className="inspector-section">
+        <div className="inspector-heading">
+          <div><h2>可重复运行</h2><p>同一配置、算法版本与 seed 应得到逐位一致的确定性结果。</p></div>
+        </div>
+        <div className="reproducibility-box">
+          <label>
+            <span>实验 seed</span>
+            <input
+              type="number"
+              step={1}
+              value={config.seed}
+              disabled={busy || busyMonteCarlo}
+              onChange={(event) => {
+                const value = event.currentTarget.valueAsNumber;
+                if (Number.isSafeInteger(value)) onSeedChange(value);
+              }}
+            />
+          </label>
+          <button type="button" disabled={busy || busyMonteCarlo} onClick={onRerun}>
+            {busy ? <LoaderCircle className="spin" size={16} /> : <RefreshCw size={16} />}
+            {dirty ? "应用参数并重算" : "按相同配置重算"}
+          </button>
+        </div>
+        <div className="export-actions">
+          <button type="button" disabled={busy} onClick={onExportScenario}><Download size={15} />导出场景 JSON</button>
+          <button type="button" disabled={busy || run === null} onClick={onExportRun}><Download size={15} />导出运行结果</button>
+        </div>
+        <p className="reproducibility-note">运行结果包含完整配置、事件、遥测、快照与最终指标；不包含任何官方型号数据声明。</p>
+      </section>
+
       <section className="inspector-section">
         <div className="inspector-heading">
           <div><h2>单场景结果</h2><p>取完整运行的最终指标，不用当前帧冒充结果。</p></div>
         </div>
+        {dirty && run !== null ? (
+          <div className="inline-warning">当前显示的是上一次已运行配置的结果；应用参数并重算后才会更新。</div>
+        ) : null}
         {run === null ? (
           <div className="inline-empty">尚未运行单场景</div>
         ) : (
@@ -592,25 +646,26 @@ function ExperimentInspector({
 
       <section className="inspector-section">
         <div className="inspector-heading">
-          <div><h2>Monte Carlo 对照</h2><p>比较固定网、α-β 与预测协同的捕获率和载荷代价。</p></div>
+          <div><h2>有界扰动对照</h2><p>每种算法使用同一组扰动样本；结果是模型内相对比较，不是实际成功概率。</p></div>
         </div>
         <div className="monte-carlo-controls">
           <label>
             <span>样本数</span>
             <select value={sampleCount} onChange={(event) => setSampleCount(Number(event.currentTarget.value))} disabled={busy || busyMonteCarlo}>
+              <option value={3}>3</option>
+              <option value={5}>5</option>
+              <option value={10}>10</option>
               <option value={20}>20</option>
-              <option value={50}>50</option>
-              <option value={100}>100</option>
-              <option value={200}>200</option>
             </select>
           </label>
           <button
             type="button"
-            disabled={onRunMonteCarlo === undefined || busy || busyMonteCarlo}
-            onClick={() => onRunMonteCarlo?.(sampleCount)}
+            className={busyMonteCarlo ? "is-cancel" : ""}
+            disabled={busy || (busyMonteCarlo ? onCancelMonteCarlo === undefined : onRunMonteCarlo === undefined)}
+            onClick={() => busyMonteCarlo ? onCancelMonteCarlo?.() : onRunMonteCarlo?.(sampleCount)}
           >
-            {busyMonteCarlo ? <LoaderCircle className="spin" size={17} /> : <PlayCircle size={17} />}
-            {busyMonteCarlo ? "批量计算中" : "运行对照"}
+            {busyMonteCarlo ? <Square size={15} fill="currentColor" /> : <PlayCircle size={17} />}
+            {busyMonteCarlo ? "取消对照" : "运行对照"}
           </button>
         </div>
         {monteCarloSummary === null ? (
@@ -624,19 +679,19 @@ function ExperimentInspector({
               <article className="comparison-row" key={variant.id}>
                 <div className="comparison-title">
                   <strong>{variant.label}</strong>
-                  <span>{variant.captures}/{variant.runs} 捕获</span>
+                  <span>{variant.secured}/{variant.runs} 稳定 · {variant.captures} 捕获</span>
                 </div>
                 <div className="comparison-measure">
-                  <span>捕获率</span>
-                  <div><i style={{ width: `${Math.max(0, Math.min(100, variant.captureRate * 100))}%` }} /></div>
-                  <strong>{(variant.captureRate * 100).toFixed(1)}%</strong>
+                  <span>稳定率</span>
+                  <div><i style={{ width: `${Math.max(0, Math.min(100, variant.securedRate * 100))}%` }} /></div>
+                  <strong>{(variant.securedRate * 100).toFixed(1)}%</strong>
                 </div>
                 <div className="comparison-measure comparison-measure--load">
-                  <span>P95 载荷</span>
-                  <div><i style={{ width: `${Math.max(0, Math.min(100, variant.p95PeakLoadG / maxLoad * 100))}%` }} /></div>
-                  <strong>{variant.p95PeakLoadG.toFixed(2)} g</strong>
+                  <span>P95 接触力</span>
+                  <div><i style={{ width: `${Math.max(0, Math.min(100, variant.p95PeakLoadKn / maxLoad * 100))}%` }} /></div>
+                  <strong>{variant.p95PeakLoadKn.toFixed(0)} kN</strong>
                 </div>
-                <small>均值 {variant.meanPeakLoadG.toFixed(2)} g · {variant.algorithm}</small>
+                <small>捕获率 {(variant.captureRate * 100).toFixed(1)}% · 接触力均值 {variant.meanPeakLoadKn.toFixed(0)} kN · {variant.algorithm}</small>
               </article>
             ))}
           </div>
@@ -663,14 +718,15 @@ function ExperimentInspector({
 }
 
 export function Inspector(props: InspectorProps) {
+  const controlsBusy = props.busy || props.busyMonteCarlo;
   if (props.tab === "scenario") {
-    return <ScenarioInspector {...props} />;
+    return <ScenarioInspector {...props} busy={controlsBusy} />;
   }
   if (props.tab === "communications") {
-    return <CommunicationsInspector {...props} />;
+    return <CommunicationsInspector {...props} busy={controlsBusy} />;
   }
   if (props.tab === "algorithm") {
-    return <AlgorithmInspector {...props} />;
+    return <AlgorithmInspector {...props} busy={controlsBusy} />;
   }
   return <ExperimentInspector {...props} />;
 }
diff --git a/apps/web/src/components/PlaybackBar.tsx b/apps/web/src/components/PlaybackBar.tsx
index 172091742109231560b798d2d691a7b8f80bd2a3..60dcadd9c9521f26b46c613ac1cbc34dd60009d3 100644
--- a/apps/web/src/components/PlaybackBar.tsx
+++ b/apps/web/src/components/PlaybackBar.tsx
@@ -1,4 +1,4 @@
-import { LoaderCircle, Pause, Play, RotateCcw } from "lucide-react";
+import { LoaderCircle, Pause, Play, RotateCcw, StepBack, StepForward } from "lucide-react";
 
 export interface PlaybackBarProps {
   currentTimeS: number;
@@ -11,6 +11,7 @@ export interface PlaybackBarProps {
   canPlay: boolean;
   onTogglePlaying: () => void;
   onReset: () => void;
+  onStep: (direction: -1 | 1) => void;
   onSeek: (timeS: number) => void;
   onSpeedChange: (speed: number) => void;
 }
@@ -28,6 +29,7 @@ export function PlaybackBar({
   canPlay,
   onTogglePlaying,
   onReset,
+  onStep,
   onSeek,
   onSpeedChange
 }: PlaybackBarProps) {
@@ -42,6 +44,16 @@ export function PlaybackBar({
           {busy ? <LoaderCircle className="spin" size={21} /> : <RotateCcw size={21} />}
           {busy ? "计算中" : "重启"}
         </button>
+        <button
+          type="button"
+          className="step-button"
+          disabled={busy || !canPlay}
+          onClick={() => onStep(-1)}
+          aria-label="后退一帧"
+          title="后退一帧"
+        >
+          <StepBack size={19} />
+        </button>
         <button
           type="button"
           className="play-button"
@@ -52,6 +64,16 @@ export function PlaybackBar({
           {playing ? <Pause size={24} fill="currentColor" /> : <Play size={24} fill="currentColor" />}
           {playing ? "暂停" : "播放"}
         </button>
+        <button
+          type="button"
+          className="step-button"
+          disabled={busy || !canPlay}
+          onClick={() => onStep(1)}
+          aria-label="前进一帧"
+          title="前进一帧"
+        >
+          <StepForward size={19} />
+        </button>
       </div>
 
       <div className="timeline-control">
diff --git a/apps/web/src/dashboard.css b/apps/web/src/dashboard.css
index abf14eb934572d53a5e865d374231e24ff10bdae..0e64c173f8e22e3c3068199b357bb5d11cac10a7 100644
--- a/apps/web/src/dashboard.css
+++ b/apps/web/src/dashboard.css
@@ -1282,3 +1282,250 @@
     transition-duration: 0.01ms !important;
   }
 }
+
+.dirty-indicator {
+  display: block;
+  margin-top: 3px;
+  color: var(--dashboard-hot);
+  font-size: 9px;
+  line-height: 1;
+  letter-spacing: 0.04em;
+}
+
+.dashboard-error {
+  position: fixed;
+  top: 82px;
+  left: 50%;
+  z-index: 40;
+  max-width: min(760px, calc(100vw - 32px));
+  padding: 10px 14px;
+  border: 1px solid rgba(255, 86, 104, 0.65);
+  border-radius: 5px;
+  color: #ffdce1;
+  background: rgba(69, 15, 25, 0.94);
+  box-shadow: 0 14px 40px rgba(0, 0, 0, 0.34);
+  font-size: 12px;
+  transform: translateX(-50%);
+}
+
+.boundary-backdrop {
+  position: fixed;
+  inset: 0;
+  z-index: 50;
+  display: grid;
+  place-items: center;
+  padding: 24px;
+  background: rgba(2, 7, 12, 0.76);
+  backdrop-filter: blur(8px);
+}
+
+.boundary-dialog {
+  width: min(920px, 100%);
+  max-height: min(760px, calc(100vh - 48px));
+  overflow: auto;
+  border: 1px solid #35516a;
+  border-radius: 8px;
+  background: #0b1722;
+  box-shadow: 0 30px 90px rgba(0, 0, 0, 0.55);
+}
+
+.boundary-dialog > header {
+  position: sticky;
+  top: 0;
+  z-index: 1;
+  display: flex;
+  align-items: flex-start;
+  justify-content: space-between;
+  gap: 20px;
+  padding: 20px 22px 17px;
+  border-bottom: 1px solid #263b4e;
+  background: rgba(11, 23, 34, 0.97);
+}
+
+.boundary-dialog h2 {
+  margin: 0;
+  color: #eff7fd;
+  font-size: 20px;
+  letter-spacing: 0.04em;
+}
+
+.boundary-dialog header p {
+  margin: 6px 0 0;
+  color: #8fa4b7;
+  font-size: 12px;
+}
+
+.boundary-dialog header button {
+  display: grid;
+  flex: 0 0 auto;
+  place-items: center;
+  width: 36px;
+  height: 36px;
+  padding: 0;
+  border: 1px solid #2f465b;
+  border-radius: 5px;
+  color: #9db0c0;
+  background: #101f2c;
+  cursor: pointer;
+}
+
+.boundary-dialog header button:hover {
+  color: #fff;
+  border-color: #4e708c;
+}
+
+.boundary-grid {
+  display: grid;
+  grid-template-columns: repeat(2, minmax(0, 1fr));
+  gap: 1px;
+  background: #203246;
+}
+
+.boundary-grid article {
+  min-height: 150px;
+  padding: 22px;
+  background: #0d1b28;
+}
+
+.boundary-grid strong {
+  color: #53c7ff;
+  font-size: 13px;
+  letter-spacing: 0.07em;
+}
+
+.boundary-grid p {
+  margin: 10px 0 0;
+  color: #a3b3c1;
+  font-size: 12px;
+  line-height: 1.75;
+}
+
+.reproducibility-box {
+  display: grid;
+  grid-template-columns: minmax(0, 1fr) auto;
+  gap: 8px;
+  align-items: end;
+}
+
+.reproducibility-box label {
+  display: grid;
+  gap: 5px;
+  color: #8598aa;
+  font-size: 9px;
+}
+
+.reproducibility-box input {
+  width: 100%;
+  height: 35px;
+  padding: 0 9px;
+  border: 1px solid #2d4255;
+  border-radius: 4px;
+  color: #dce8f1;
+  background: #0a1621;
+  font-family: "Cascadia Mono", Consolas, monospace;
+  font-size: 11px;
+}
+
+.reproducibility-box button,
+.export-actions button {
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  gap: 6px;
+  height: 35px;
+  padding: 0 10px;
+  border: 1px solid #31516a;
+  border-radius: 4px;
+  color: #b9dff5;
+  background: rgba(31, 100, 143, 0.12);
+  font-size: 9px;
+  font-weight: 650;
+  cursor: pointer;
+}